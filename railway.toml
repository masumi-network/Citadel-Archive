[build]
builder = "RAILPACK"

[deploy]
startCommand = "python scripts/run_railway.py"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

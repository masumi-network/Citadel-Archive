[build]
builder = "RAILPACK"

[deploy]
startCommand = "sh -c 'uvicorn kb.server:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
